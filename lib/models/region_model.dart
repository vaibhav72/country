import 'package:flutter/material.dart';

class RegionModel {
  String regionName;
  Color regionThemeColor;
  Color regionTextColor;
  RegionModel({this.regionName, this.regionTextColor, this.regionThemeColor});
}
